# Media

Release 1.0 photographs captured on real HP 200LX hardware:

- `login.jpg`: MINIX startup completed at the login prompt.
- `logged-in.jpg`: root shell using the persistent native filesystem.
- `boot-sequence.jpg`: the DOS loader immediately before entering MINIX.
- `volts.jpg`: Mack's battery-voltage utility running under MINIX.
